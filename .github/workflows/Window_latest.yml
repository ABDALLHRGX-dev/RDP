name: Windows latest

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Install Tailscale
        shell: powershell
        run: |
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12

          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-1.96.3-amd64.msi"
          $file = "$env:TEMP\tailscale.msi"

          curl.exe -L $url -o $file

          Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet /norestart" -Wait

      - name: Start Tailscale
        shell: powershell
        run: |
          $ts = "$env:ProgramFiles\Tailscale\tailscale.exe"

          & $ts up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-rdp-$env:GITHUB_RUN_ID

          for ($i = 0; $i -lt 20; $i++) {
            Start-Sleep 5
            $ip = & $ts ip -4
            if ($ip) { break }
          }

          echo "RDP_IP=$ip" >> $env:GITHUB_ENV

      - name: Download Playit
        shell: powershell
        run: |
          $url = "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.26/playit-windows-amd64.exe"

          curl.exe -L --retry 5 --retry-delay 5 $url -o playit.exe

          if (!(Test-Path ".\playit.exe")) {
            throw "playit.exe missing"
          }

      - name: Start Playit
        shell: powershell
        run: |
          Start-Process -WindowStyle Hidden -FilePath ".\playit.exe"

          Start-Sleep 20

      - name: Enable RDP & Open Ports
        shell: powershell
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force

          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force

          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Lsa" -Name "LimitBlankPasswordUse" -Value 0 -Force

          netsh advfirewall firewall add rule name="RDP" dir=in action=allow protocol=TCP localport=3389

          netsh advfirewall firewall add rule name="UDP7777" dir=in action=allow protocol=UDP localport=7777

          netsh advfirewall firewall add rule name="TCP7777" dir=in action=allow protocol=TCP localport=7777

          Restart-Service TermService -Force

      - name: Create User
        shell: powershell
        run: |
          $password = ConvertTo-SecureString "Abc!2024!*1*1*1*" -AsPlainText -Force

          New-LocalUser "RDP" -Password $password -AccountNeverExpires

          Add-LocalGroupMember -Group "Administrators" -Member "RDP"

          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"

      - name: Show Info
        shell: powershell
        run: |
          Write-Host "IP: $env:RDP_IP"
          Write-Host "User: RDP"
          Write-Host "Pass: Abc!2024!*1*1*1*"
          Write-Host "Playit Running"

          while ($true) {
            Start-Sleep 300
          }
